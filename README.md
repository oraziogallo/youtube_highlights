Prerequisites:
```pip install yt-dlp```
```ffmpeg```

Open this folder in the terminal.
Run:
```
python3 -m http.server 8000
```
Open a browser and point it to: http://localhost:8000/video-editor.html